# Every plugin pinned to a specific commit SHA. `antidote update` skips
# pinned bundles. To bump:
#   1. Pick the new SHA (`git ls-remote https://github.com/<repo>` for HEAD,
#      or `git ls-remote https://github.com/<repo> refs/tags/<tag>^{}` for an
#      annotated tag, or just `refs/tags/<tag>` for a lightweight tag).
#   2. Edit the pin: line below.
#   3. `rm -rf ~/.cache/antidote/github.com/<repo> ~/.zsh_plugins.zsh && exec zsh`

# Fast compinit replacement (prezto: completion module). No upstream tags.
mattmc3/ez-compinit pin:5c9b6da88a463573e26e6846ff164026640ef2f6

# Extra completion definitions — was at tag 0.36.0
zsh-users/zsh-completions kind:fpath path:src pin:28c5bdcaf81bb89e56d0df8267d822c3b8aed9e0

# fzf-driven tab completion menu (must load after compinit but before plugins
# that wrap widgets like zsh-autosuggestions / fast-syntax-highlighting).
# Requires the `fzf` binary on PATH. Was at tag v1.3.0.
Aloxaf/fzf-tab pin:d7e0234614dbe5369fdd760907d12c0e05a4dccc

# Inline suggestions from history — was at tag v0.7.1
zsh-users/zsh-autosuggestions pin:e52ee8ca55bcc56a17c828767a3f98f22a68d4eb

# Up/Down history-prefix search (prezto: history-substring-search) — was at tag v1.1.0
zsh-users/zsh-history-substring-search pin:400e58a87f72ecec14f783fbd29bc6be4ff1641c

# Syntax highlighting on the command line (prezto: syntax-highlighting). Must be last.
# was at tag v1.56 (annotated)
zdharma-continuum/fast-syntax-highlighting pin:5ecd353c81214f82bdeca5483fab6ccc5a2d5494
